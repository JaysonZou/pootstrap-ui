import Input from '../lib/input';

import React, { FormEvent, useState } from 'react';

export default {
  title: 'Input',
  component: Input 
};

export const Primary = () => {
  return <Input />
};

export const Addon = () => {
  return <Input />
}